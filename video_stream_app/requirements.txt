# Video Stream Analyzer - Backend Dependencies

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.0
pydantic-settings>=2.0.0
pymysql>=1.1.0  # MySQL driver for SQLAlchemy

# Video Processing
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# OpenAI API (GPT & TTS)
openai>=1.12.0

# Local VLM Model (vLLM + Swift)
# ms-swift provides VllmEngine for Qwen2.5-VL
# Install with: pip install ms-swift vllm
# Note: Requires compatible CUDA version

# Optional: SAM2 (install separately if needed)
# pip install sam2

# Async Support
aiofiles>=23.2.0
httpx>=0.26.0
websockets>=12.0

# WebRTC Streaming (optional - for built-in WebRTC support)
# Full stream simulator available at: /data2/jj/proj/video_processor/rtc_simulator/
aiortc>=1.6.0
aiohttp>=3.9.0

# Development
python-dotenv>=1.0.0

